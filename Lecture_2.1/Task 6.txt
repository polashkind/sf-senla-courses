6) Определите, что будет выведено на экран в результате выполнения следующего кода(важно понять, почему):
Boolean b1 = false;
system.debug(b1); // false
Boolean b2 = true;
system.debug(b2); // true
system.debug(b1 == b2); // false
system.debug(b1 != b2); // true
